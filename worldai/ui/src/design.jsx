import React from 'react'
import ReactDOM from 'react-dom/client'
import DesignClient from './DesignClient.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DesignClient />
  </React.StrictMode>,
)
